# ----------------------------------------------------------------------------------------------------------------------
#
# ----------------------------------------------------------------------------------------------------------------------
spring:
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: "jdbc:mysql://127.0.0.1:3306/sys?useSSL=false&allowPublicKeyRetrieval=true"
    username: "root"
    password: "root"
    hikari:
      connection-test-query: "SELECT 'mysql-helper'"
